# Local development runs against the demo API without Clerk.
VITE_API_BASE_URL=http://localhost:4000
# Required in production. Publishable key only; never add a Clerk secret key to the frontend.
VITE_CLERK_PUBLISHABLE_KEY=
